apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ingestion-service-monitor
  namespace: ai-agents
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: ingestion-service
  endpoints:
  - port: http
    path: /metrics
    interval: 5s